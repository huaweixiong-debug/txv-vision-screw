# Claude Code 强执行版提示词

把下面整段直接发给 Claude Code：

```text
你现在不要分析通讯，不要排查扫码，不要碰后端。只做一件事：把生产页布局改得更美观、更稳、更像工业 HMI。

项目路径：
S:\expansion_valve_hmi

你必须先阅读：
- S:\expansion_valve_hmi\docs\production-layout.md

然后直接改代码，不要只给建议。允许修改：
- S:\expansion_valve_hmi\web\index.html
- S:\expansion_valve_hmi\web\styles.css
- S:\expansion_valve_hmi\web\app.js

绝对不要修改：
- 通讯逻辑
- PLC 逻辑
- 扫码逻辑
- 相机逻辑
- API
- 设置页
- records / datasets 页

这次唯一目标：
把当前生产页从“很多卡片堆在一起，看着乱、主次不清、像后台系统”改成“工业设备主操作界面，重点明确、视觉稳定、第一眼就能看懂”的页面。

你必须遵守这些硬要求：

1. 不改任何业务逻辑
只改布局、样式、默认面板位置、局部 DOM 结构整理。

2. 不要破坏现有绑定
现有 id、按钮 id、状态节点 id 尽量保留。
不要把现有 JS 依赖的选择器改坏。

3. 保留拖拽和缩放能力
生产页现在支持卡片拖拽和缩放，这个功能不能删。
但默认布局必须重新设计得更整齐，做到“不拖也顺眼”。

4. 必须强制刷新默认布局
修改 S:\expansion_valve_hmi\web\app.js 里的 DEFAULT_LAYOUT。
同时升级 LAYOUT_STORAGE_KEY，例如从 hmi_production_layout_v2 改到 hmi_production_layout_v3 或更高，确保旧缓存失效。

5. O 型圈视觉必须成为主角
用户第一眼必须看到 O 型圈视觉，而不是左侧流程卡或顶部碎信息。

6. 最近记录必须变成稳定右侧栏
不要再像一个普通表格块，它应该像右侧长期存在的信息面板。

页面改版目标如下：

一、整体结构
- 左列：操作员、PLC、扫码，作为辅助工具区
- 中列：O 型圈视觉，作为主工作区
- 右列：最近记录，作为稳定信息栏
- 中列下方：奇力速拧紧，与视觉区上下对齐

二、视觉主次
- 最强：O 型圈视觉
- 第二强：当前步骤 / 当前状态
- 第三强：最近记录
- 最弱：左侧工具区、辅助按钮、帮助文字

三、风格要求
- 更像工业 HMI，不像管理后台
- 更整齐，不要像一堆漂浮卡片
- 少一点花哨，多一点秩序
- 保持现代感，但不要“玻璃拟态过重”

四、必须解决的具体问题
- 顶部摘要现在太像第二个主界面，要收敛
- 左侧三张卡片风格不统一，要做成同一组工具区
- 中间视觉卡片内部 KPI 太碎，要收敛成更清楚的结果摘要
- 右侧最近记录现在存在感不对，要改成稳定边栏
- 圆角、阴影、渐变太多，要减弱
- 面板之间对齐关系不稳定，要用更严格的栅格感

五、默认布局请按这个方向改

可以直接采用或非常接近下面这组默认值：

const DEFAULT_LAYOUT = {
  "operator-panel":   { x: 0.00, y: 0.00, w: 0.21, h: 0.24 },
  "plc-panel":        { x: 0.00, y: 0.25, w: 0.21, h: 0.33 },
  "scan-panel":       { x: 0.00, y: 0.59, w: 0.21, h: 0.17 },
  "camera-panel":     { x: 0.22, y: 0.00, w: 0.44, h: 0.60 },
  "tightening-panel": { x: 0.22, y: 0.61, w: 0.44, h: 0.18 },
  "recent-panel":     { x: 0.67, y: 0.00, w: 0.33, h: 0.79 },
};

原则是：
- 左窄、中宽、右稳
- 中列最大
- 右栏完整
- 视觉区和拧紧区上下对齐

六、顶部摘要区怎么改
- hero-card 保留，但变成真正的“当前步骤主摘要”
- 两张 metric-card 弱化装饰，作为安静的上下文信息块
- automation-bar 不要再像独立大卡片，做成一条更扁平的工具条
- 顶部区域整体更紧凑，减少视觉噪声

七、O 型圈视觉卡片怎么改
- 图片画面优先级最高
- 识别数量做成最大的数字
- 右侧只保留少量关键状态，不要四五个同权重小块互相竞争
- 判定信息要一眼看懂
- 手动测试按钮收紧到底部，不要抢主视觉

八、左侧工具区怎么改
- 操作员、PLC、扫码三张卡片改成同一视觉系统
- 标题更小、更干净
- 内边距更紧
- 按钮高度统一
- 字段和状态项排列规整
- 让人感觉这是“辅助控制列”，不是三张主卡片

九、最近记录怎么改
- 做成安静、稳定、完整的右侧栏
- 表头固定
- 行高统一
- 标签颜色收敛
- 刷新按钮缩小
- 不要比中间视觉区更抢眼

十、样式上怎么收敛
- 适当减小圆角
- 明显减轻阴影
- 减少玻璃模糊感
- 非主卡片去掉厚重渐变
- 统一间距和字号层级

建议方向：
- 主卡片圆角 16px-18px
- 次卡片圆角 12px-14px
- 面板内边距 12px-14px
- 小组件间距 6px-8px
- 关键数字 28px-40px
- 普通标题 14px-16px
- 辅助文字 11px-12px

十一、index.html 的改动边界
可以整理生产页局部结构，但必须克制：
- 可以合并视觉区内部重复 KPI 容器
- 可以调整顶部摘要区内部顺序
- 可以减少冗余包裹层
- 不要大改 id
- 不要让 app.js 现有取元素逻辑失效

十二、最终交付要求
你改完以后，请直接给出：
- 修改了哪些文件
- 为什么这样改
- 新默认布局是什么
- 有没有升级 LAYOUT_STORAGE_KEY
- 页面视觉层级是怎么处理的

十三、验收标准
改完后必须满足：
- 第一眼看到 O 型圈视觉
- 第二眼看到当前步骤和记录
- 左侧辅助区不再喧宾夺主
- 页面看起来整齐、稳定、统一
- 不再像很多卡片随手拼起来
- 默认布局不拖拽也已经顺眼

如果你发现 production-layout.md 和当前页面实现有冲突，以 production-layout.md 的改版目标为准，但仍然不要碰业务逻辑。

直接开始改代码，不要先输出长篇分析。
```
