# =============================================================================
# 膨胀阀拧紧防错追溯 HMI — 完整依赖
# pip install -r requirements.txt
# =============================================================================

# --- YOLO 实时推理 ---
ultralytics>=8.0.0
opencv-contrib-python>=4.6.0     # WeChat QRCode + cv2 图像处理
numpy>=1.24

# --- 设备通讯 ---
python-snap7>=3.0.0              # Siemens S7-200 SMART PLC
pyserial>=3.5                    # 扫码枪 (COM口)

# --- QR 解码 ---
zxing-cpp>=3.0.0                 # 多格式条形码/QR解码

# --- Web 服务 (标准库，无需额外安装) ---
# http.server, json, pathlib, threading, etc.

# --- 部署工具 (仅开发机需要) ---
paramiko>=3.0.0                  # SSH/SFTP 远程部署
